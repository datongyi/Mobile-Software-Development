/** 云开发使用 */
export const cloudbaseTemplateConfig = {
  // Pin the environment used by the deployed mini program. Leaving this
  // empty can make DevTools and the experience version use different envs.
  env: 'cloud1-d5gpui3epda8ace92',
  photoUseMock: false,
  allowPhotoFallback: false,
};
